# DatingClient

Кроссплатформенное мобильное приложение для знакомств, разработанное на базе **MAUI (.NET 8.0)** с поддержкой iOS, Android.

## 📋 Содержание

- [Обзор](#обзор)
- [Технологический стек](#технологический-стек)
- [Архитектура](#архитектура)
- [Структура проекта](#структура-проекта)
- [Требования к системе](#требования-к-системе)
- [Установка и настройка](#установка-и-настройка)
- [Основные возможности](#основные-возможности)
- [Архитектура сервисов](#архитектура-и-сервисы-данных)
- [Build и развертывание](#Build-и-развертывание)

**📚 Дополнительно:** см. [`README_EXTENDED.md`](README_EXTENDED.md) для подробных примеров кода и решения проблем

## Обзор

Функционал:

- 🔐 Авторизация и управление профилем
- 👥 Просмотр профилей других пользователей с применением фильтров
- ❤️ Лайки и просмотр полученных лайков
- 💬 Чат с другими пользователями в реальном времени
- 🗂️ Кэширование аватаров и данных профилей

## Технологический стек

### Основные фреймворки и библиотеки:

| Компонент                 | Версия  | Назначение                       |
| ------------------------- | ------- | -------------------------------- |
| **.NET**                  | 8.0     | Платформа разработки             |
| **MAUI**                  | Latest  | Кроссплатформенный UI фреймворк  |
| **CommunityToolkit.Maui** | 9.1.1   | Расширенные MAUI компоненты      |
| **CommunityToolkit.MVVM** | 8.3.0   | MVVM паттерны и инструменты      |
| **Newtonsoft.Json**       | 13.0.3  | JSON сериализация/десериализация |
| **sqlite-net-pcl**        | 1.9.172 | Локальное хранилище данных       |

### Целевые платформы:

- 🤖 **Android** 5.0+
- 🍎 **iOS** 11.0+

## Архитектура

Проект использует паттерн **MVVM (Model-View-ViewModel)**:

```
┌─────────────────────────────────────────┐
│           User Interface                │
│    (Views: XAML + Code-Behind)          │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│        ViewModel Layer                   │
│  (Business Logic, State Management)      │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│        Services Layer                    │
│ (ApiService, SocketService, CacheService)
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│      Models & Data Access                │
│    (SQLite, REST API, WebSocket)         │
└─────────────────────────────────────────┘
```

## Структура проекта

```
DatingClient/
├── 📄 App.xaml                    # Точка входа приложения
├── 📄 App.xaml.cs                 # Логика инициализации
├── 📄 AppShell.xaml               # Маршрутизация приложения
├── 📄 Constants.cs                # Глобальные константы
├── 📄 MauiProgram.cs              # Конфигурация DI контейнера
│
├── 📁 Converters/                 # XAML конвертеры
│   ├── BoolToColorConverter.cs     # Конвертация bool → Color
│   ├── BoolToLayoutConverter.cs    # Конвертация bool → StackLayout
│   ├── GenderJsonConverter.cs      # JSON сериализация пола
│   ├── GenderToStringConverter.cs  # Gender → String
│   ├── InverseBoolConverter.cs     # Инверсия boolean
│   └── NullableToBoolConverter.cs  # null → false конвертация
│
├── 📁 Helpers/                    # Вспомогательные классы
│   ├── AuthHelper.cs              # Работа с аутентификацией
│   ├── GenderHelper.cs            # Работа с полом пользователя
│   ├── QueryHelper.cs             # Построение query параметров
│   └── StorageHelper.cs           # Работа с локальным хранилищем
│
├── 📁 Models/                     # Модели данных
│   ├── Auth.cs                    # Аутентификационные данные
│   ├── ChatSummary.cs             # Краткая информация о чате
│   ├── IUpdatable.cs              # Интерфейс обновляемых моделей
│   ├── LocationSuggestion.cs      # Предложение местоположения
│   ├── Message.cs                 # Сообщение в чате
│   ├── SearchFilter.cs            # Фильтры поиска
│   ├── User.cs                    # Полный профиль пользователя
│   ├── UserProfile.cs             # Базовая информация пользователя
│   └── WSMessage.cs               # WebSocket сообщение
│
├── 📁 Services/                   # Бизнес-логика и сервисы
│   ├── ApiService.cs              # REST API клиент
│   ├── AvatarCacheService.cs      # Кэширование аватаров
│   ├── CacheService.cs            # Общее кэширование данных
│   ├── LocationService.cs         # Работа с геолокацией
│   └── SocketService.cs           # WebSocket соединение
│
├── 📁 Utils/                      # Утилиты
│   └── LimitedDeque.cs            # Двусторонняя очередь с лимитом
│
├── 📁 ViewModels/                 # ViewModel слой
│   ├── ChatsViewModel.cs          # VM списка чатов
│   ├── LikesViewModel.cs          # VM лайков
│   ├── LoginViewModel.cs          # VM авторизации
│   ├── MessagesViewModel.cs       # VM сообщений в чате
│   ├── ProfilePopupViewModel.cs   # VM попап профиля
│   ├── ProfileViewModel.cs        # VM профиля пользователя
│   ├── SearchViewModel.cs         # VM поиска профилей
│   └── SettingsViewModel.cs       # VM настроек
│
├── 📁 Views/                      # UI слой (XAML + Code-Behind)
│   ├── ChatsPage.xaml(.cs)        # Страница чатов
│   ├── FilterPopup.xaml(.cs)      # Попап фильтров поиска
│   ├── LikesPage.xaml(.cs)        # Страница лайков
│   ├── LoadingOverlay.xaml(.cs)   # Оверлей загрузки
│   ├── LoginPage.xaml(.cs)        # Страница входа
│   ├── MessagesPage.xaml(.cs)     # Страница чата
│   ├── ProfilePage.xaml(.cs)      # Страница профиля
│   ├── ProfilePopup.xaml(.cs)     # Попап профиля
│   ├── SearchPage.xaml(.cs)       # Страница поиска
│   └── SettingsPage.xaml(.cs)     # Страница настроек
│
├── 📁 Resources/                  # Ресурсы приложения
│   ├── AppIcon/                   # Иконки приложения
│   ├── Fonts/                     # Пользовательские шрифты
│   ├── Images/                    # Изображения
│   ├── Raw/                       # Сырые ресурсы
│   ├── Splash/                    # Заставка приложения
│   └── Styles/                    # XAML стили
│
├── 📁 Platforms/                  # Платформенный код
│   ├── Android/                   # Android специфические файлы
│   └── iOS/                       # iOS специфические файлы
│
└── 📁 Properties/
    └── launchSettings.json        # Параметры запуска
```

## Требования к системе

### Для разработки:

- **Visual Studio** или **Rider**
- **.NET 8.0 SDK**
- **Java Development Kit (JDK)** 11+ (для Android)
- **Android SDK** (API Level 21+)
- **Xcode** 14+ (для iOS, требуется macOS)

### Минимальные требования платформ:

- **Android:** API Level 21 (5.0 Lollipop)
- **iOS:** версия 11.0 и выше

## Установка и настройка

### 1. Клонирование репозитория

```bash
git clone https://github.com/dmMorsh/DatingClient.git
cd DatingClient
```

### 2. Восстановление зависимостей

```bash
dotnet restore
```

### 3. Настройка конфигурации

Отредактируйте `Constants.cs` и установите URL вашего API сервера:

```csharp
public const string ApiBaseUrl = "http://192.168.0.12:8088";
// или для production
public const string ApiBaseUrl = "https://your-domain.com";
```

### 4. Сборка проекта

```bash
# Сборка для всех платформ
dotnet build

# Или для конкретной платформы
dotnet build -f net8.0-android
dotnet build -f net8.0-ios
```

### 5. Запуск приложения

```bash
# Android
dotnet build -t Run -f net8.0-android

# iOS (требуется macOS)
dotnet build -t Run -f net8.0-ios
```

## Основные возможности

### 🔐 Аутентификация и управление токенами

**Функциональность:**

- Регистрация и вход в учетную запись
- Управление токенами доступа и refresh токенами
- Сохранение данных сеанса в локальном хранилище
- Автоматическое обновление токена при истечении

**Логика:**

```
Пользователь вводит credentials
           ↓
API возвращает access_token + refresh_token
           ↓
Токены сохраняются в SecureStorage
           ↓
При каждом запросе добавляется Bearer token в заголовок
           ↓
Если получена 401 ошибка → автоматический Refresh
           ↓
Повтор оригинального запроса с новым токеном
```

**Нюансы реализации:**

- `ApiService.SendWithRetry()` автоматически обновляет токен при 401 ошибке
- В `DEBUG` конфигурации используется локальный API адрес из `Constants.cs`
- В `RELEASE` конфигурации используется адрес из `Preferences` (позволяет менять сервер без перекомпиляции)
- Refresh токен отправляется в теле POST запроса на `/refresh`

**⚠️ Возможные улучшения:**

- Добавить механизм проактивного обновления токена (за 1-2 минуты до истечения)
- Реализовать логирование попыток refresh для отладки
- Добавить максимальное количество попыток refresh перед выходом из приложения

**Файлы:** `LoginViewModel.cs`, `AuthHelper.cs`, `Auth.cs`, `ApiService.cs`

---

### 👥 Карусель профилей и поиск с фильтрацией

**Функциональность:**

- Карусель свайпа для просмотра профилей
- Применение множественных фильтров (пол, возраст, местоположение, расстояние)
- Сохранение фильтров между сеансами
- Восстановление позиции свайпа (undo функция)

**Глубокая логика работы:**

#### 1. Инициализация фильтров

```csharp
// При первом запуске фильтры заполняются данными пользователя
Filter = new SearchFilter
{
    InterestedIn = me.Gender,           // Какой пол ищет пользователь
    Gender = me.InterestedIn,           // Пол пользователя
    MinAge = me.Age - 1,
    MaxAge = me.Age + 1,
    Latitude = me.Latitude,             // Текущее местоположение
    Longitude = me.Longitude,
    MaxDistanceKm = 10,                 // Радиус поиска
};
StorageHelper.SaveSearchFilter(Filter); // Сохранить в локальное хранилище
```

#### 2. Структура данных поиска

Используется **LimitedDeque** (двусторонняя ограниченная очередь):

- **Основной буфер:** максимум 50 профилей для свайпа
- **Undo буфер:** последние 5 действий можно отменить
- **Current pointer:** текущий просматриваемый профиль

```
[User1] ← [User2] ← [User3] (Current) → [User4] → [User5]
         ↑_______UNDO BUFFER (5 items)_______↑
         ↑___________MAIN BUFFER (50 items)____________↑
```

#### 3. Поиск с пагинацией

```csharp
const int PageSize = 20;  // Запрашивается 20 профилей за раз
_lastSeenId              // ID последнего полученного профиля (для курсора)

// Логика:
1. Запрос идёт только когда буфер заканчивается (< 5 элементов)
2. Отправляется ID последнего профиля для получения следующей партии
3. Предотвращает бесконечное количество запросов
4. При _endReached = true больше запросов не идут
```

**Нюансы:**

- Фильтры сохраняются при каждом применении, не теряются при перезагрузке
- `LocationPlaceholder` показывает либо текущий ввод, либо сохранённое местоположение

**⚠️ Возможные улучшения:**

- Подогнать значения буферов, размера страницы и т.д.
- Добавить фильтрацию по интересам/хобби (требует расширения `SearchFilter`)
- Реализовать "умный поиск" — если профилей осталось мало, автоматически расширять радиус
- Оптимизировать загрузку аватаров (загружать низкое разрешение по умолчанию, полное при клике)

**Файлы:** `SearchViewModel.cs`, `SearchPage.xaml`, `LimitedDeque.cs`, `SearchFilter.cs`

---

### ❤️ Система лайков и матчей

**Функциональность:**

- Отправка лайка профилю
- Просмотр полученных лайков
- Автоматические уведомления о взаимных лайках (матчах)

**Логика взаимодействия:**

```
Пользователь A свайпает вправо на Пользователя B
           ↓
API endpoint: POST /swipe { target_id: userId, action: "like" }
           ↓
Сервер проверяет: а не лайкнул ли B уже A?
           ↓
                    ┌─ ДА → MATCH!
                    │      Обе стороны получают уведомление через WebSocket
                    │      Создаётся чат между ними
                    │
                    └─ НЕТ → просто like
```

**Нюансы:**

- После отправки лайка профиль сдвигается с карусели
- Если матч произойдёт, пользователю показывается алерт (из SocketService)
- Система предотвращает двойные лайки через API (400 Bad Request)

**⚠️ Возможные улучшения:**

- Автоматически обновлять страницу чатов при получении "match", сейчас нужно протягивать вниз.
- Добавить "супер лайк" с дополнительным сообщением
- Показывать аналитику: кто лайкнул, откуда, когда

**Файлы:** `LikesViewModel.cs`, `LikesPage.xaml`

---

### 💬 Чаты и сообщения в реальном времени

**Функциональность:**

- Список активных чатов с превью последних сообщений
- Отправка и получение сообщений в реальном времени
- WebSocket синхронизация между клиентом и сервером
- История сообщений

**Архитектура WebSocket:**

#### Инициализация подключения:

```csharp
1. SocketService.ConnectAsync()
   ↓
2. API запрос: POST /ws/start → получить sessionToken
   ↓
3. WebSocket URL: ws://api.host/ws/chat?session={token}
   ↓
4. ClientWebSocket.ConnectAsync() → подключение
   ↓
5. ListenAsync() → фоновый поток слушает входящие сообщения
```

#### Жизненный цикл сообщения:

```
Пользователь отправляет сообщение по HTTP POST
           ↓
Сервер обрабатывает и рассылает адресату по WebSocket
           ↓
ListenAsync() получает событие
           ↓
Десериализация JSON → WSMessage
           ↓
OnMessageReceived?.Invoke() → все подписчики получают уведомление
           ↓
MessagesViewModel и ChatsViewModel обновляют ObservableCollection
           ↓
UI автоматически обновляется
```

**Типы WebSocket событий:**

- `Constants.Message` — текстовое сообщение
- `Constants.Match` — уведомление о матче (показывается алерт)

**Нюансы реализации:**

1. **Обработка ошибок:**

   - При ошибке десериализации показывается алерт с содержимым JSON
   - При разрыве соединения предпринимается попытка повторного подключения

2. **Состояние подключения:**
   - Глобальный флаг `_isConnected` предотвращает переиспользование подключения при смене пользователя

**⚠️ Возможные улучшения:**

- Реализовать "heartbeat" пинги для проверки живого соединения
- Добавить индикатор статуса подключения в UI (connected/disconnected)
- Сохранять сообщения локально, если соединение потеряно, отправлять при восстановлении
- Добавить шифрование сообщений на стороне клиента

**Файлы:** `ChatsViewModel.cs`, `MessagesViewModel.cs`, `SocketService.cs`, `WSMessage.cs`

---

### 📍 Геолокация и сервис местоположения

**Функциональность:**

- Определение текущего местоположения устройства
- Автоматическое определение города/страны
- Предложения локаций при вводе

**Логика работы:**

```
LocationService.GetCurrentLocationAsync()
           ↓
Запрос разрешения у ОС (первый раз)
           ↓
GPS модуль определяет координаты (Latitude, Longitude)
           ↓
Обратный геокодинг → Название города (через API)
           ↓
Сохранить в профиль пользователя
           ↓
Использовать в фильтрах поиска
```

**Автодополнение локаций:**

```
Пользователь вводит "Мос..."
           ↓
LocationService.SearchLocationsAsync("Мос")
           ↓
API запрос к сервису геокодирования
           ↓
LocationSuggestion[] возвращаются в Suggestions
           ↓
UI показывает список: "Москва", "Мосточная", и т.д.
           ↓
При выборе: Location = "Москва", Latitude/Longitude обновляются
```

**Нюансы:**

- Разные разрешения для Android/iOS (требует платформенный код в `/Platforms/`)
- Может требовать несколько секунд при первом определении
- Сохраняется в `Preferences` для последующих сеансов

**⚠️ Возможные улучшения:**

- Добавить fallback на IP-based геолокацию (если GPS не доступен)
- Реализовать кэширование результатов поиска локаций
- Добавить возможность скрыть точное местоположение (показывать город/регион, но не расстояние)

**Файлы:** `LocationService.cs`, `LocationSuggestion.cs`, `Platforms/`

---

### ⚙️ Настройки профиля

**Функциональность:**

- Редактирование личной информации (имя, возраст, пол, интересы)
- Изменение предпочтений поиска (возраст, пол, расстояние)

**Логика обновления профиля:**

```
Пользователь изменяет поле (например, ник)
           ↓
SettingsViewModel.OnPropertyChanged()
           ↓
RelayCommand срабатывает при сохранении
           ↓
API запрос: PUT /me
           ↓
Сервер возвращает обновлённый профиль
           ↓
Локальный кэш обновляется (CacheService)
           ↓
UI отображает новые значения
```

**Нюансы:**

- Все изменения сохраняются на сервер (нет локального "черновика")
- При ошибке загрузки данные откатываются к последним известным
- Изображения загружаются отдельно через multipart/form-data

**⚠️ Возможные улучшения:**

- Реализовать управление аватаром и фотографиями
- Добавить локальное хранилище черновиков профиля перед отправкой
- Реализовать оптимистичное обновление UI (изменить локально, потом синхронизировать)
- Добавить валидацию на клиенте перед отправкой
- Показывать индикатор "изменения не сохранены"

**Файлы:** `SettingsViewModel.cs`, `SettingsPage.xaml`

## Архитектура и сервисы данных

### 1️⃣ ApiService.cs — REST API клиент

**Назначение:** Единая точка доступа ко всему REST API, управление HTTP соединением, токенами и повторными попытками.

**Жизненный цикл запроса:**

```
1. HTTP запрос инициируется
   ↓
2. AddAuthHeader() добавляет Bearer token
   ↓
3. SendWithRetry() выполняет запрос
   ↓
4. Статус 200-299? → успех, вернуть результат
   ↓
5. Статус 401? → попытка обновить токен
   ├─ Успешное обновление → повторить запрос
   └─ Ошибка обновления → вернуть 401 (нужен новый вход)
   ↓
6. Другие ошибки → вернуть HttpResponseMessage
```

**Ключевые нюансы:**

1. **Debug vs Release конфигурация:**

   - В DEBUG используется локальный адрес из `Constants.ApiBaseUrl`
   - В RELEASE используется адрес из `Preferences` (можно менять без перекомпиляции)
   - Это позволяет одной сборкой поддерживать разные серверы

2. **Автоматическое обновление токена:**

   - При 401 ошибке пытается обновить токен через `/refresh` endpoint
   - Повторяет запрос один раз с новым токеном
   - Если обновление не удалось → 401 пользователю

**⚠️ Возможные улучшения:**

   - Добавить логирование запросов (сложно отлаживать)
   - Добавить loggin и exponential backoff
   - Установить timeout = 10 сек для быстрого fail

**Файлы:** `ApiService.cs`, `Constants.cs`

---

### 2️⃣ CacheService.cs — Двухуровневое кэширование

**Назначение:** Оптимизация производительности через локальное кэширование профилей.

**Архитектура:**

```
Level 1: ConcurrentDictionary (RAM) — быстро (~1ms), но теряется при перезагрузке
                    ↓
Level 2: SQLite (Disk) — медленнее (~50-100ms), но персистентен
                    ↓
Level 3: API (Network) — самый медленный (~500-2000ms)
```

**Жизненный цикл запроса профиля:**

```
1. Есть в RAM и не устарел (< 2 дней)? → вернуть (~1ms)
2. Нет? Есть в SQLite и не устарел?  → загрузить в RAM и вернуть (~50ms)
3. Нет? Загрузить с API → сохранить в оба кэша (~500ms+)
4. Ошибка API? → вернуть заглушку или последний известный профиль
```

**Оптимизация с аватарами:**

- При получении профиля загружается аватар один раз
- URL заменяется на локальный путь (из `AvatarCacheService`)
- Результат: 95%+ трафика сохраняется при загрузке

**Проблема:**

- Кэш действует 2 дня — если профиль изменился, пользователь увидит старые данные
- План: использовать разные TTL для разных типов данных

**Файлы:** `CacheService.cs`, `IUpdatable.cs`

---

### 3️⃣ SocketService.cs — WebSocket для реал-тайма

**Назначение:** Управление постоянным соединением для сообщений в чатах и уведомлений.

**Цикл подключения:**

```csharp
1. Запрос session token: POST /ws/start
2. Преобразовать HTTP → WebSocket URL
3. ClientWebSocket.ConnectAsync()
4. Запустить фоновый ListenAsync() (не блокирует UI)
5. Слушать входящие сообщения в цикле
```

**⚠️ КРИТИЧНЫЕ ПРОБЛЕМЫ:**

1. **Нет heartbeat пингов**

   - Проксии могут разорвать "мертвое" соединение
   - Нужно отправлять пинг каждые 120 сек для проверки живого соединения

**Файлы:** `SocketService.cs`, `WSMessage.cs`

---

### 4️⃣ AvatarCacheService.cs — Оптимизация изображений

**Назначение:** Локальное кэширование аватаров для снижения трафика.

**Логика:**

```
AvatarUrl: "https://api.host/avatars/user123.jpg"
     ↓
GetAvatarPathAsync(url)
     ├─ На диске? → вернуть локальный путь
     └─ Нет? → загрузить, сохранить, вернуть
     ↓
UI использует локальный путь (0 трафика при повторе)
```

**Экономия трафика:**

- Первый просмотр профиля: 50-200KB
- Повторный просмотр: 0KB (из кэша)
- **Результат:** 95%+ сбережение при загрузке

**⚠️ Улучшения:**

- Добавить сжатие изображений при сохранении
- Реализовать лимит размера кэша (удалять старые, если > 100MB)
- Использовать WebP для меньшего размера файлов

**Файлы:** `AvatarCacheService.cs`

---

### 5️⃣ LocationService.cs — Геолокация

**Назначение:** Определение местоположения, поиск по координатам, автодополнение.

**Процесс:**

```
1. GetCurrentLocationAsync()
   ↓
2. Запрос разрешения ОС (первый раз)
   ↓
3. GPS определяет Latitude/Longitude (~5-50м точность)
   ↓
4. Обратный геокодинг: координаты → название города (optional)
   ↓
5. Сохранить в профиль
```

**Нюансы:**

- Требует платформенный код в `/Platforms/Android/` и `/Platforms/iOS/`
- Поиск локаций работает через API автодополнение

**⚠️ Улучшения:**

- Добавить fallback на IP-based локацию (не требует GPS разрешения)
- Кэшировать координаты, обновлять только если > 1км отклонения

**Файлы:** `LocationService.cs`, `LocationSuggestion.cs`

---

## Build и развертывание

### Для Android

```bash
# Debug
dotnet publish -f net8.0-android -c Debug

# Release
dotnet publish -f net8.0-android -c Release
```

**Выходной файл:** `bin/Release/net8.0-android/com.companyname.datingclient.apk`

### Для iOS

```bash
# Требуется macOS
dotnet publish -f net8.0-ios -c Release
```

## Решение проблем

### Проблема: "Unable to connect to API"

**Решение:**

- Проверьте значение `ApiBaseUrl` в `Constants.cs`
- Убедитесь, что API сервер запущен и доступен
- Проверьте сетевое соединение устройства

### Проблема: "WebSocket connection failed"

**Решение:**

- Проверьте URL WebSocket сервера
- Убедитесь, что порт открыт в файерволе

---